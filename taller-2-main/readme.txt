Ya se puede leer el hash desde el visual, pero no todos los objetos lo tienen, tampoco se puede acceder a la info desde details.js.

Ya accede a la info con el hash, el hash viene del id y se genera automaticamente al crear un producto

Se usa un metodo para meter el hash a cada elemento creado previamente

El metodo setIdAll se usara para meter una foto a cada elemento

Como se hace el carrito? ver video

*** .  TODO ************************

meter el css y materialize a las pantallas: create, signin, signup

Poner boton de agregar al carrito

Poner en el formulario el boton para agregar foto